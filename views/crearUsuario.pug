<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en Uptask

            include partials/mensajes


            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu email" id="email" value= email)

                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu password" id="password" value= password)

                .campo.enviar
                    input.boton(type="submit" value="Crear cuenta")

                .campo
                    a(href="/iniciar-sesion") Volver a iniciar sesión

        script(src="/dist/bundle.js")
